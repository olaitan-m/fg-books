import styled from 'styled-components';

export const CollectionCategory = styled.div`
	margin: 3rem 0;

	.title {
		font-size: 3rem;
		margin-bottom: 35px;
		text-align: center;
		text-decoration: underline;
		text-transform: uppercase;
	}
	.items {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
	}
`;
